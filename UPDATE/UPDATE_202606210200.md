2026/06/21 02:00
The Scaler Overwrite: I removed the redundant StandardScaler() initialization in the RL script. The script now correctly transforms the RL environment's features using the exact statistical weights saved during the deep learning phase, keeping the AI's probability confidence intact.

Purged Train/Validation Split: I subtracted the max_horizon window from the end of the training set. This guarantees the model's early stopping mechanism doesn't accidentally validate itself on overlapping future price regimes, securing a truthful out-of-sample test.

Stationary State Space: Instead of dividing floating PnL by the expanding account balance (which shrinks the metric towards 0.0 as you make money), the PnL is now normalized by the market's True Range (atr). This keeps the RL agent's mathematical perspective consistent from trade 1 to trade 1,000.